import React from 'react';
import {Container} from "react-bootstrap";

const Point = () => {
    return (
        <React.Fragment>
            <Container>

            </Container>
        </React.Fragment>
    );
};

export default Point;